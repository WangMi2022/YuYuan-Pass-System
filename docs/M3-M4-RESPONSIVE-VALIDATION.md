# M3/M4 生产响应式验收记录

> 验收日期：2026-08-14
> 生产入口：`http://192.166.20.103:8080/`
> 响应式修复版本：`1a5e0f1e4829940cdd7886db6428c413753f74d5`
> 文档基线版本：`4b4f886d0a0d728aac3271ab69a38697f0e2e5c3`

## 1. 验收范围

本次补充验证两个已发布页面：

- M3 发票识别质量：`#/layout/invoiceCenter/invoiceQuality`
- M4 智能建档：`#/layout/assetCenter/assetRecognition`

验证目标是确认响应式修复后，页面根节点、主要容器、筛选器、按钮和表格在指定尺寸下不存在页面级横向溢出。任务抽屉仅在存在真实识别任务时可打开，因此单独记录其验收状态。

## 2. 验收方法与结论口径

本次使用生产页面 DOM 快照和浏览器几何测量，记录 `window.innerWidth`、`document.documentElement.scrollWidth`、`document.body.scrollWidth` 以及关键控件边界。

- `document/body.scrollWidth` 不大于 `innerWidth`：判定没有页面根级横向溢出。
- 表格内容宽于容器但由组件内部滚动区域承载：不判定为页面根级溢出。
- DOM/几何测量只能证明响应式结构和边界，不等同于完整截图视觉验收。
- 截图接口持续报错 `Timed out running CDP command "Page.captureScreenshot"`，因此本文不把未取得截图的档位写成“视觉截图通过”。

## 3. M3 发票识别质量

| 视口 | 根页面宽度 | 结果 | 备注 |
| --- | --- | --- | --- |
| `1440×1000` | 无根级溢出 | 通过 | 已完成页面视觉检查，Provider、分类、字段指标和失败列表正常 |
| `900×900` | 无根级溢出 | 通过 | 已完成页面视觉检查，筛选器、指标卡和表格边界正常 |
| `390×844` | `document/body.scrollWidth = 390` | 几何通过 | Provider、分类、字段指标和失败列表均正常渲染；表格横向内容由内部滚动容器承载 |

M3 三档响应式结构均通过。手机档已完成 DOM 和几何验证，但由于截图能力异常，尚未取得可归档的手机视觉截图。

## 4. M4 智能建档空态页面

| 视口 | 根页面宽度 | 关键测量 | 结果 |
| --- | --- | --- | --- |
| `1440×1000` | `document/body.scrollWidth = 1440` | Header、刷新按钮、上传区域和任务表格均在页面右边界内 | 几何通过 |
| `900×900` | `document/body.scrollWidth = 900` | 页面 `0 → 900`；Header `32 → 868`；上传/任务面板 `32 → 868`；刷新按钮 `761.4 → 866`；任务表格 `48.8 → 851.2` | 几何通过 |
| `390×844` | `document/body.scrollWidth = 390` | Header 自动纵向排列；刷新按钮 `14 → 118.6`；识别、清空、状态筛选和任务表格宽度均约 `336.8` | 几何通过 |

M4 空态页面三档均不存在页面级横向溢出。标题、按钮、上传区域、状态筛选和任务表格的响应式规则已生效。

## 5. M4 任务抽屉状态

当前生产任务列表为空，任务抽屉 DOM 节点处于关闭状态，宽高均为 `0`。因此以下有任务状态仍未完成真实页面验收：

- 图片证据区域。
- 结构化草稿表单。
- 字段置信度和 OCR 长文本。
- 重复候选提醒。
- 抽屉底部“保存草稿”“确认建档”“放弃任务”等操作区。
- `1440×1000`、`900×900`、`390×844` 三档抽屉边界和折行。

完成该项需要上传一张测试铭牌并触发一次生产 Vision 调用，等待任务进入 `reviewing`，验收后再通过正式删除接口清理任务和临时图片。该动作会写入生产数据并产生模型调用费用，必须单独确认后执行。

## 6. 最终结论

- M3：三档响应式结构通过；桌面双档已有视觉检查，手机档为 DOM/几何通过，截图待补。
- M4：三档空态页面 DOM/几何通过；没有发现需要重新发布的页面级响应式缺陷。
- M4 抽屉：未取得真实任务状态，不能声明通过。
- 截图：浏览器 CDP 截图持续超时，不能用几何数据冒充截图证据。
- 后续唯一阻塞项：经确认后创建并清理一次生产测试任务，补齐 M4 抽屉三档验收；截图能力恢复后补充归档图片。
